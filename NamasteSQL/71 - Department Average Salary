select d.department_name,round(avg(e.salary),2) as average_salary  from Departments d 
join Employees e on e.department_id  = d.department_id group by d.department_name
having count(e.employee_id)>2
order by average_salary desc
