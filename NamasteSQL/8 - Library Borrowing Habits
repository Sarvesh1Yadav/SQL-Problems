select br.BorrowerName,group_concat(b.BookName order by b.BookName separator ", ") as BorrowedBooks              
from Borrowers br join Books b on b.BookID  = br.BookID
group by br.BorrowerName
order by br.BorrowerName
