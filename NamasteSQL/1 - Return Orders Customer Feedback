select o.customer_name ,round((count(r.order_id )*100/count(distinct o.order_id )),2) as
return_percent from orders o left join returns r on r.order_id     = o.order_id    
group by o.customer_name
having return_percent>50
order by o.customer_name
